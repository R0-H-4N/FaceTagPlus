# Database Credentials (Required)
DB_USER=fullstack
DB_USER_PASS=your_secure_password_here

# JWT Secret (Required - Change this!)
# Must be at least 32 characters
JWT_SECRET=change_this_to_a_random_secure_string_at_least_32_characters_long

# Optional: Node environment (defaults to production)
NODE_ENV=production
